# Steming
added the code in c for stemming which is the principle of categorizing the words of same origin to a one single stem.
# Stop words
words that create a problem while creating inverted index as they consume a lot of space and create wide spaced gaps in the documets and most importantly are of no use while ranking the document .so these words are suppose to be removed from the article before it is indexed
